Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Login>=0.6,<1.0
python-dotenv>=1.0,<2.0
reportlab>=4.0,<5.0
anthropic>=0.7,<1.0

# DeepSec (document / malware analysis) engine — merged in Phase 1 of the DeepSec stitch.
# Flask, Werkzeug and reportlab are already covered by AppSec's ranges above, so only
# the document-parsing and YARA libraries are added here.
PyPDF2==3.0.1
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
Pillow>=11.0            # reportlab already pulls a 12.x Pillow; range avoids a downgrade
requests>=2.32,<3.0
# NOTE: yara-python removed — nothing in this codebase imports `yara`.
# The docscan "YARA" scanner (backend/services/docscan/scanners/yara_scanner.py)
# is pure-Python string matching. Re-add yara-python only if real YARA rule
# support is implemented (Windows: use Python <= 3.13 for prebuilt wheels).
pikepdf>=9.0,<10.0      # structural PDF sanitizer: parse-and-rebuild (replaces regex approach)

# ─── Production serving (see DEPLOY.md) ───
# The Flask dev server in run.py is dev-only; hosts start the app via the Procfile.
gunicorn>=23.0,<24.0
# Only used when DATABASE_URL points at Postgres (free Neon/Supabase tier).
# Harmless on the default SQLite path — SQLAlchemy never loads it.
psycopg2-binary>=2.9,<3.0
